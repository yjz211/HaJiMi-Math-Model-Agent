# HaJiMi adapter: modeling-paper-standard

This is a compiled HaJiMi capability, not a Codex skill discovery entry. Runtime routing injects short fragments under a context budget; detailed contracts and deterministic checkers remain available under `resources/` on demand. Resolve `{capabilityRoot}` to this directory. Use WSL/Python paths and task-relative paper paths.

All reader-visible paper content must come from active frozen claims and evidence. New facts, assumptions, formulas, numbers, or model decisions trigger rollback to the owning workflow stage. A generated paper remains `candidate` until user review in stage 9.
