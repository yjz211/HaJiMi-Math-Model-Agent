Write the Chinese mathematical-modeling paper in LaTeX from active frozen Claim/Evidence only. Start with a claim-evidence map and per-question section plan; never invent or hand-retype key numbers. Use the fixed competition-paper order, explicit Chinese/decimal numbering, no contents page, and an abstract of at most one page whose question paragraphs each close problem-method-result. Use direct, concrete competition language and keep internal workflow, hashes, stages, gates, and evaluator names out of reader-facing prose. Build by question, compile, and keep the result `candidate` until stage-9 user review.

Follow the bundled SKILL.md task-specific reading route. Reuse unchanged rules already read; load section-specific instructions when working on that section. For new papers use resources/scripts/create_paper_skeleton.py with the real question count, paper type and optimization questions. Keep active frozen evidence, per-question inputs, paper/hajimi-paper-config.json, claim bindings and hajimi_validate_delivery in the same governed stage-8 workflow. Preserve requirements-contract.md writing/template standards. Delivery uses stage8-policy.md light review, not mandatory historical audit loops. Local corrections do not restart whole-paper drafting.

Create `paper/hajimi-paper-config.json` with task-relative `mainTex`, `finalPdf`, and `figureManifest`, plus `questions`, `paperType`, 2–6 `topicTerms`, and each optimization question's coverage-matrix `constraintMin`. HaJiMi delivery validation uses this file to invoke the adapted paper and plot gates.
