# 阶段 9：人工验收后的材料生成与打包

用户明确验收第8阶段正文后，调用本阶段 modeling-submission-package 能力。通过 hajimi_generate_submission 执行内置生成器，输入该能力契约格式的 configJson，源路径相对本题根目录。程序分配新版本交付目录，不允许普通 write/edit/bash 修改已验收正文、代码、冻结证据或历史包。

生成同一正文的不含代码版和含代码附录版、必要代码副本与中文附录映射、复现说明、真实记录支撑的 AI 使用详情 DOCX/PDF、SHA-256 清单和 ZIP。AI说明事实不足时只生成明确草稿；不能编造人工核验或官方合规。package 成功并登记 ZIP 后阶段直接完成，不再建立包验证、页面预览或审查链。题意、约束、结果或代码需改动时由用户提出返工，旧提交材料保留但不可再视为最新版。
