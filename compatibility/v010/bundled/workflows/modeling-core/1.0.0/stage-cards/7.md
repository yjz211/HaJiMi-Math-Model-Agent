# 阶段 7：证据与展示结果整理
将执行绑定输入、代码、参数、环境与输出；证据必须经过确定性和领域验证。把正式 Claim 绑定到 Evidence，再创建冻结集。无法追溯或已 stale 的数字、结论与图形不得进入阶段 8。

阶段结束、进入第8阶段前，在对话中询问：「需要什么风格的图像：鲜艳舒适型，还是稳重科研型？」两种模式均在此等待用户选择；此前已明确选择则沿用并说明，不重复询问。鲜艳舒适型对应 expressive，稳重科研型对应 restrained。把用户原话保留为第8阶段方向，进入第8阶段后读取 modeling-plot-suite 的 profiles/<selection>.md 并应用现有项目配色/风格标记；不另创第三套风格。
