Use deterministic source for exact topology, mathematical geometry, engineering mechanisms, model flows, networks, and schedules. Nodes and arrows must encode real semantics; ordinary labels are frameless. A full-paper overview flowchart should map problem decomposition, model routes, solution, validation, and per-question outputs, but local diagrams are included only when they reduce genuine comprehension cost. Preserve editable source and vector PDF, run the format-specific checker, and visually inspect geometry and text.
