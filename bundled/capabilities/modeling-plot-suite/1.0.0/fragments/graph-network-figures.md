For graph and network claims, preserve exact topology and direction with deterministic source. Encode node and edge meaning, weights, communities, paths, cuts, or flows only when they are present in frozen data. For dense networks prefer an evidence-oriented matrix, distribution, or filtered view over an unreadable hairball; if topology itself is the claim, keep labels, legends, and layout rules reproducible.
