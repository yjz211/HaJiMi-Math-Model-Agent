For every optimization question, place Decision variables → Objective → Constraints → Model summary inside “模型建立” as Step 1–4, followed by peer sections for solution and result analysis. Explain each variable group, objective component, weight/unit, and each semantic constraint class before the complete summary formula. Maintain a problem-formula-variable-code-evidence constraint coverage matrix. The solution section must explain mapping/encoding, initialization or parameters, feasibility handling, and stopping; results must include the executable plan, objective components, constraint satisfaction, and at least one comparison or bound.
