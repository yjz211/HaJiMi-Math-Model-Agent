用户现在授权修复刚完成的16张图，按当前HaJiMi新增规则进行一轮集中修复。此前只允许首次输出、不审图不修改的限制仅针对上一轮，现在由本请求明确取代。本次只修图，不写论文、不进入writing、不结束整个阶段。

继续使用当前Windows受管后端、当前真实模型和当前任务已有16图、FIGURE_PLAN.json、生成器与冻结证据。不得重新求解、修改数据或减少16个图件的内容覆盖。使用HaJiMi内置旧原版skill实现，不用全局迁移skill。
先读取当前bundled modeling-plot-suite/1.0.0/stage8-policy.md、profiles/expressive.md、upstream-planning.md，及原版相关绘图指南与配方。按原版最终尺寸公式预计算，136 mm引用宽度。先把figures目录、FIGURE_PLAN.json、PAPER_PLAN.md及RESULT.md复制备份到repair-before（若已有不可覆盖）。

先打开原有16张PNG，集中识别标签/图例/批注/面板轴文字的重叠，逐图写REPAIR_PLAN.md：全部冲突、最终锚点/偏移/边距安排和设计选择。只集中评估一轮然后统一修改，不边改边试，不反复微调。重点关注Q2热图与右侧剖面之间文字拥挤，但仍需看完16图。
- 流程图/方框结构图用同一主色及浅色，不逐节点彩虹配色。
- 工程/物理图现有颜色保留；批注框全部白底、细中性边框，检查引线与文字避让。
- expressive不仅鲜艳，必须积极考虑新颖图型和有意义的复合图：每张DATA图比较两个有真实数据支持的表达方式，写清采用的组合及信息增益或保留单图的理由。已有好组合可保留；对普通常规表达积极改成互补组合或新颖图型。不能仅换色，也不为凑新颖加虚构分布、置信区间、数据或装饰面板。不规定机械改图数量。保持真实几何比例与当前明亮舒适风格。
- 图型设计变化同步计划与配方。若改FIGURE_PLAN.json，按现有上游校验工具重新validate通过再执行；不要重新begin导致丢失现有计划。
- 只处理图内布局，不处理论文落页、float和分页。

统一修改源码后每个受影响图仅进行一次成功导出（语法/工具错误可排除）。未经修改无需强行重绘。不要用运行所有生成器的方式反复覆盖已成功修复的图。完成后仅快速看一遍并记录残余问题，不启动第二轮修复、不跑历史全套审计。保留PDF+PNG+可编辑生成器。
最后写REPAIR_RESULT.md，逐图列修改/保留、新颖组合选择、输出与残余问题，标明这是一次修复候选而不是全部通过审计。验证16图输出齐全与冻结证据未变后停止；不要写论文，不自动进入下一子阶段。

补充当前用户要求：本轮要认真评估哪些图能升级为强复合图。宿主已看三页总览，建议重点候选：Q3布线（空间主图+线距边缘剖面+真实局部放大）、Q4地形（空间地形+对应剖面+坡度分布）、Q4覆盖（共享空间坐标+离散覆盖+局部细节/真实覆盖比例）、约束权衡（趋势+目标空间或选定候选联动）、Pareto（主散点+边缘分布+前沿局部放大）、角度敏感性（风险+重优化代价联动）。逐一核对真实数据再决定，不必须全采纳，不只是平铺互不关联小图，不拼凑装饰。16个输出ID和信息覆盖保留。优先清晰关系、共享尺度、选定对象跨面板一致高亮，136mm仍须可读。
运行时已补HAJIMI_RUNTIME_HOME。此前bash全部在执行前被配置阻塞，没有成功备份或修图；确认状态后继续当前这一轮，已读不变内容不用重复读。
