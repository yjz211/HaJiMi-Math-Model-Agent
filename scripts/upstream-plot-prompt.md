用户授权：从当前隔离目录中的前七阶段材料，自主重新规划完整图集，再一次性出图。不是沿用旧六图任务。此目录来自第7阶段结束检查点及对应CAS原件，未复制第8阶段论文、脚本或绘图产物。历史阶段7 publication_manifest仅用于证据治理，不是本轮清单；不要读取旧图片来模仿。

先完整读取 __CAPABILITY__/upstream-planning.md 和 __CAPABILITY__/profiles/expressive.md。用read/ls/find/grep读取reports/stage-1到stage-7、问题合同、逐问求解代码、机器结果、验证与冻结证据；读取原版figure_exemplars.md、figure_style_guide.md。自主确定图数、图型、配方和组合。不得套用以前fig_compare六主题清单。数值和结论边界保持真实，不重跑求解，不把淘汰方案当正式结论。

完整图集按上游标准先写FIGURE_PLAN.json并调用hajimi_validate_figure_plan validate。shell当前已被规划门禁暂停，通过后才能运行。规划期读取输入PDF若需要shell解析，先用已有问题合同/源码/报告规划，通过后再提取原题核对；不得为逃避检查乱填计划。DATA>=3，12–20只是软参考，本题按真实推理/数据密集程度和空间/推导触发要求安排；不凑数、不遗漏关键结果或验证。每张写具体图型、配方或custom、选择理由、一句核心信息、章节、实际存在的source路径、136mm最终引用宽和PDF/PNG输出。路线图和真实几何推导图另计，必须按触发要求安排。

使用expressive鲜艳激进模式。CLAUDE.md已固定配色和版式，在数据支持时主动考虑新颖单图、同一坐标系内多图层融合图，也鼓励有信息增益的多面板组合图；不默认多面板优先，不以子图数量衡量新颖。融合图最终为一套坐标轴，各图层共享坐标语义。保持明亮视觉层次，具体执行当前 expressive.md。规划通过后按原版完整工作流，派生同一清单的PAPER_PLAN.md锚定FIGURE_MANIFEST，执行分类验证、bootstrap、配方批量预取和逐图绘制。调用真实受管后端。数据图、DrawIO路线图和TikZ推导图按原版对应渲染路径；不要调用图像生成服务或制作插画。

只保留每张图第一次成功导出。阻止首次导出的字段/导入/语法错误可处理；成功图片不重绘。不打开图片审查，不运行出图后审计，不修改图，不写论文正文。完整输出后在RESULT.md列实际图数、图型、配方和输出路径，立即停止。不要声称视觉检查通过。
